[testhost]
# Keep the container target identity explicit.  The Docker connection plugin
# otherwise inherits the controller remote user, which makes CI depend on the
# runner account/UID and breaks become inside the test image.
test-container ansible_connection=docker ansible_user=root ansible_python_interpreter=/usr/bin/python3
