---
# Handlers for horde_stats_exporter role

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart horde-exporter
  ansible.builtin.systemd:
    name: horde-exporter
    state: restarted
  become: true
